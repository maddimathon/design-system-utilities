---
/**
 * @since ___PKG_VERSION___
 *
 * @packageDocumentation
 */

/* import - types */

/* import - functions */
import ColourTable_Scripts from '../../../astro/support/ColourTable_Scripts.astro';

/* import - values */
import tokens from '../../../../dist/default-tokens.json';

/* import - components */

/* import - layouts */
import ColourTable from '../../../astro/components/ColourTable.astro';
import DocsPage from '../../layouts/DocsPage.astro';
import ColourList from 'src/astro/components/ColourList.astro';

/* TYPES ===================================== */

/* CONSTANTS ===================================== */
---

<DocsPage
    title='Colour Tokens'
    layout='extra-wide'
>
    <ColourTable
        id='all-colours'
        colour={tokens.colour}
    />

    {
        (Object.keys(tokens.colour) as (keyof typeof tokens.colour)[]).map(
            (name) => (
                <h2 set:html={name} />
                <ColourList
                    bases={tokens.colour.base}
                    colour={tokens.colour[name]}
                    id={`${name}-shade-list`}
                    name={name}
                    shadeHeading="3"
                />
            )
        )
    }
</DocsPage>

<ColourTable_Scripts />
