---
/**
 * @since 0.1.0-alpha
 *
 * @packageDocumentation
 */

/* import - types */
import type { ComponentProps } from 'astro/types';
import type { SvgMaker } from '../../ts/01-utilities/SvgMaker.js';

/* import - functions */

/* import - values */

/* import - components */
import UtilsAlert from '@maddimathon/utility-astro/components/Alert';
// import Icon from './Icon.astro';

/* import - layouts */

/* PROPS ===================================== */

export interface Props
    extends Omit<
        ComponentProps<typeof UtilsAlert>,
        'svg' | 'svgLabel' | 'type'
    > {
    icon: SvgMaker.JsonReturn;
}

const { icon, class: classList = [], ...attributes }: Props = Astro.props;

/* CONSTANTS ===================================== */
---

<UtilsAlert
    class:list={{ 'alert-block--has-heading': attributes.heading }}
    slug={icon.slug}
    svg={icon.svg}
    svgLabel={icon.label}
    {...attributes}
>
    <slot
        slot='headline'
        name='headline'
    />
    <slot />
</UtilsAlert>
