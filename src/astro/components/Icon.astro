---
/**
 * @since ___PKG_VERSION___
 *
 * @packageDocumentation
 */

/* import - types */
import type { ComponentProps } from 'astro/types';
import type { Tokens_Icons } from '../../ts/02-tokens/Tokens_Icons.js';

/* import - functions */

/* import - values */

/* import - components */
import UtilsIcon from '@maddimathon/utility-astro/components/Icon';

/* import - layouts */

/* TYPES ===================================== */

/* PROPS ===================================== */

export interface Props
    extends Omit<
        ComponentProps<typeof UtilsIcon>,
        'svg' | 'svgLabel' | 'type'
    > {
    icon: Tokens_Icons.SvgIcon.JsonReturn;
}

const { icon, ...attributes }: Props = Astro.props;

/* CONSTANTS ===================================== */
---

<UtilsIcon
    slug={icon.slug}
    svg={icon.svg}
    svgLabel={icon.label}
    {...attributes}
/>
