/**
 * @since ___PKG_VERSION___
 * 
 * @packageDocumentation
 */
/*!
 * @maddimathon/design-system-utilities@___CURRENT_VERSION___
 * @license MIT
 */

// import type { Test } from '@maddimathon/utility-typescript/types';

import { describe, test } from '@jest/globals';
// import * as z from 'zod';

// import tokenValue from './value.js';

describe( 'W3C.atoms', () => {

    test( 'value', () => {

        // any should really be anything non-nullable
        // const values = {
        // };

        // double-checks that the types match
        // type _Tests = [
        //     Test.Expect<Test.Exactly<t_val_any[ '$value' ], any>>,
        //     Test.Expect<Test.Exactly<t_val_number[ '$value' ], number>>,
        // ];

        // only here so that these are used and don’t throw errors
        // true as _Tests[ 0 ];
    } );
} );
