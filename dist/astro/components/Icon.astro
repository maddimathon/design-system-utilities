---
/**
 * @since 0.1.0-alpha
 *
 * @packageDocumentation
 */

/* import - types */
import type { ComponentProps } from 'astro/types';
import type { SvgMaker } from '../../ts/01-utilities/SvgMaker.js';

/* import - functions */

/* import - values */

/* import - components */
import UtilsIcon from '@maddimathon/utility-astro/components/Icon';

/* import - layouts */

/* PROPS ===================================== */

export interface Props extends Omit<
    ComponentProps<typeof UtilsIcon>,
    'svg' | 'svgLabel' | 'type'
> {
    icon: SvgMaker.JsonReturn;
}

const { icon, ...attributes }: Props = Astro.props;

/* CONSTANTS ===================================== */
---

<UtilsIcon
    slug={icon.slug}
    svg={icon.svgInlineLabelled}
    svgLabel={icon.ariaLabel}
    {...attributes}
/>
